import type { EmployeeMonthRecord } from "../../../../packages/core/src/index"; import { getMonthProgressStatus, type MonthProgressStatus } from "./month-record-progress";  export type MonthRecordYearViewMonth = {   taxMonth: number;   status: MonthProgressStatus;   statusLabel: string;   isRecorded: boolean;   salaryIncome: number;   withheldTax: number; };  export type MonthRecordYearViewQuarter = {   quarter: number;   label: string;   months: MonthRecordYearViewMonth[];   summary: {     completed: number;     draft: number;     notStarted: number;   }; };  const quarterLabels = ["第一季度", "第二季度", "第三季度", "第四季度"] as const;  const getStatusLabel = (status: MonthProgressStatus) => {   switch (status) {     case "completed":       return "已完成";     case "draft":       return "待补录";     default:       return "未开始";   } };  export const buildMonthRecordYearView = (   records: EmployeeMonthRecord[], ): MonthRecordYearViewQuarter[] =>   quarterLabels.map((label, index) => {     const quarter = index + 1;     const months = [...records]       .filter((record) => Math.ceil(record.taxMonth / 3) === quarter)       .sort((left, right) => left.taxMonth - right.taxMonth)       .map((record) => {         const status = getMonthProgressStatus(record);         return {           taxMonth: record.taxMonth,           status,           statusLabel: getStatusLabel(status),           isRecorded: status !== "not_started",           salaryIncome: record.salaryIncome,           withheldTax: record.withheldTax,         };       });      return {       quarter,       label,       months,       summary: {         completed: months.filter((month) => month.status === "completed").length,         draft: months.filter((month) => month.status === "draft").length,         notStarted: months.filter((month) => month.status === "not_started").length,       },     };   });