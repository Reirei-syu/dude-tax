import assert from "node:assert/strict"; import test from "node:test"; import type { EmployeeMonthRecord } from "../../../../packages/core/src/index"; import { buildMonthRecordYearView } from "./month-record-year-view";  const createMonthRecord = (   overrides: Partial<EmployeeMonthRecord> = {}, ): EmployeeMonthRecord => ({   id: null,   unitId: 1,   employeeId: 1,   taxYear: 2026,   taxMonth: 1,   status: "incomplete",   salaryIncome: 0,   annualBonus: 0,   pensionInsurance: 0,   medicalInsurance: 0,   occupationalAnnuity: 0,   housingFund: 0,   supplementaryHousingFund: 0,   unemploymentInsurance: 0,   workInjuryInsurance: 0,   withheldTax: 0,   infantCareDeduction: 0,   childEducationDeduction: 0,   continuingEducationDeduction: 0,   housingLoanInterestDeduction: 0,   housingRentDeduction: 0,   elderCareDeduction: 0,   otherDeduction: 0,   taxReductionExemption: 0,   remark: "",   createdAt: null,   updatedAt: null,   ...overrides, });  test("整年视图按季度分组并保持月份顺序", () => {   const yearView = buildMonthRecordYearView([     createMonthRecord({ taxMonth: 1, status: "completed" }),     createMonthRecord({ taxMonth: 2, salaryIncome: 8500 }),     createMonthRecord({ taxMonth: 3 }),     createMonthRecord({ taxMonth: 4, status: "completed" }),     createMonthRecord({ taxMonth: 5 }),     createMonthRecord({ taxMonth: 6, salaryIncome: 9200 }),     createMonthRecord({ taxMonth: 7 }),     createMonthRecord({ taxMonth: 8 }),     createMonthRecord({ taxMonth: 9, status: "completed" }),     createMonthRecord({ taxMonth: 10 }),     createMonthRecord({ taxMonth: 11 }),     createMonthRecord({ taxMonth: 12 }),   ]);    assert.equal(yearView.length, 4);   assert.deepEqual(     yearView.map((quarter) => quarter.label),     ["第一季度", "第二季度", "第三季度", "第四季度"],   );   assert.deepEqual(yearView[0].months.map((month) => month.taxMonth), [1, 2, 3]);   assert.deepEqual(yearView[1].months.map((month) => month.taxMonth), [4, 5, 6]);   assert.deepEqual(yearView[0].summary, {     completed: 1,     draft: 1,     notStarted: 1,   });   assert.deepEqual(yearView[1].summary, {     completed: 1,     draft: 1,     notStarted: 1,   }); });  test("整年视图包含月份状态文案和录入标记", () => {   const yearView = buildMonthRecordYearView([     createMonthRecord({ taxMonth: 1, status: "completed" }),     createMonthRecord({ taxMonth: 2, salaryIncome: 7000 }),     createMonthRecord({ taxMonth: 3 }),   ]);    assert.deepEqual(yearView[0].months.map((month) => month.statusLabel), [     "已完成",     "待补录",     "未开始",   ]);   assert.deepEqual(yearView[0].months.map((month) => month.isRecorded), [     true,     true,     false,   ]); });