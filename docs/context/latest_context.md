# 当前上下文摘要

## 当前阶段

- Execution

## 当前任务

- 开发态与安装版数据库隔离已完成，当前进入安装包升级迁移冒烟验证准备阶段

## 已完成

- `npm run dev` 默认使用仓库内 `data/dev/dude-tax.dev.db`
- 安装版 managed API 优先使用安装目录 `data/dude-tax.db`
- 安装目录不可写时回退到 `userData`
- 若安装目录无库但旧 `userData` 库存在，首次升级会自动复制迁移
- 发布前脚本已新增数据库路径检查
- desktop/api 针对数据库路径策略的回归测试已通过

## 关键决策

- 开发库与安装库必须永久隔离
- 安装版优先保证“跟随安装路径”，但不能因为权限问题阻塞启动
- 旧库迁移采用复制不删除，先保数据安全

## 当前问题

- 真实安装包升级场景仍缺人工验证
- `.gitignore` 已忽略但仍被追踪的 Agent 文档尚未清理

## 当前测试状态

- smoke: 脚本级通过，真实安装包验证待补
- unit: 通过
- integration: 通过
- e2e: 发布前脚本已覆盖路径验证
- regression: 通过

## 下一步计划

1. 执行真实安装包升级迁移冒烟验证
2. 清理已追踪的 Agent 内部文档
3. 评估桌面包体与 Vite bundle warning 优化
