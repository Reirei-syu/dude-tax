# 当前上下文摘要

## 当前阶段

- Execution

## 当前任务

- 完成录入模型精简优化后的文档同步与最终验证

## 已完成

- 其他收入替代补发收入，补扣税调整退出主流程
- 记录状态不再作为计算前置条件
- 工作台新增只读减除费用列，显示当前政策 `basicDeductionAmount`
- 结果确认允许空白月直接确认，并在结果/历史查询中按零值月参与计算
- 月度数据录入、快速计算、结果确认、历史查询、统一导出已同步切到新口径
- 自动化验证通过：
  - `npm test`
  - `npm run typecheck`
  - `npm run build --workspace @dude-tax/desktop`
  - `npm run build --workspace @dude-tax/api`

## 剩余任务

- 在 Windows / Electron 环境下补 4 个主页面的手工烟测

## 关键决策

- 其他收入保留金额与备注，移除补扣税调整和所属期间
- 工作台不再展示记录状态
- 已确认月份即视为有效月份；无录入内容时按零值月参与计算
- 减除费用只读预填，不开放编辑，不修改系统维护配置入口

## 当前问题

- Legacy 单月接口与导入仍保留少量旧字段兼容别名，后续如果继续清理，需要单独做删除计划
- 仍未完成桌面壳人工烟测；已尝试用 Playwright 做本地预览烟测，但被当前环境的浏览器 profile 权限限制阻断

## 下一步计划

1. 在桌面壳中手工检查月度数据录入、快速计算、结果确认、历史查询
2. 根据烟测结果决定是否继续清理 legacy 单月接口与旧辅助工具
