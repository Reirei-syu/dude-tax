# 当前上下文摘要

## 当前阶段

- Execution

## 当前任务

- 完成系统维护、单位年份体系、员工状态与月度录入联动优化。

## 已完成

- 清理了本轮涉及页面与接口的主要运行时乱码。
- 新增单位年份持久化表 `unit_years`，支持起始年份、手动新增年份、删除无数据年份。
- 顶部年份选择器改为按当前单位已有年份展示。
- 新增共享员工状态推导逻辑，员工管理页显示“在职 / 离职”。
- 月度录入页改为顶部月份按钮、当前月摘要、按月份过滤离职员工。
- 批量导入模板改为真实文件下载，模板内容为简体中文表头。
- 系统维护升级为结构化政策内容维护，新增“当前政策”页面。

## 剩余任务

- 做一次更完整的人工 UI 冒烟，重点检查结果中心、历史查询、快速计算等未重写页面的文案与样式一致性。

## 关键决策

- 年份不再使用固定 `2023-2028` 候选列表，而是使用单位级 `availableTaxYears`。
- 新单位默认只创建起始年份，其余年份由用户手动新增。
- 员工状态不落库，统一按共享 helper 推导。
- 政策说明继续复用原 `maintenance_notes` 存储位，但内部改为结构化 JSON，兼容旧文本内容。

## 当前问题

- 本轮已完成自动化验证，但未做全路由逐页人工点检。

## 下一步计划

1. 逐页人工检查结果中心、历史查询、快速计算是否仍存在存量乱码。
2. 根据实际体验继续收口细节样式和文案。
